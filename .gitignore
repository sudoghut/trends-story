serp_token.txt
trending_searches.json
_trending_searches.json

# skip vscode settings
.vscode/

# Claude Code configuration
.claude/

# Configuration files with sensitive credentials
# Users should copy config.example.yaml to config.yaml and add their tokens
config.yaml

# Docker runtime files
# These files are created during container execution and should not be committed
# Lockfile to prevent concurrent executions
.run.lock
# Timestamp file tracking last successful run
.last_run
# Log files directory
logs/
# Windows command artifact from testing
nul

# Binary data files (managed locally, not via git)
images/
trends_data.db